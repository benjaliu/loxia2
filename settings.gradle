include 'api', 'core', 'support'
def project_prefix = 'loxia-'
project(':api').name = "${project_prefix}api"
project(':core').name ="${project_prefix}core"
project(':support').name ="${project_prefix}support"