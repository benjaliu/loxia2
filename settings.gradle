include 'api', 'core', 'support'

def project_name = 'loxia2'

project(':api').name = "${project_name}-api"
project(':core').name ="${project_name}-core"
project(':support').name ="${project_name}-support"